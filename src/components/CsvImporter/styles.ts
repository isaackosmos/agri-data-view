import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    borderBottomWidth: 1,
    borderBottomColor: "#ccc",
    alignItems: "center",
  },
  fileName: {
    marginTop: 5,
    fontSize: 12,
    color: "#666",
  },
});
